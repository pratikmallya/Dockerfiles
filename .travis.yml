language: bash

sudo: required

services:
  - docker

env:
  - MAKE_TASK=build

script:
  - make $MAKE_TASK

deploy:
  - provider: script
    script: make push
    skip_cleanup: true
    on:
      branch: master
      condition: ${MAKE_TASK} == build
